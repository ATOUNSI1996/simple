#include <stdio.h>







int main()
{
    FILE* fichier = NULL;

 /*   fichier = fopen("test.txt", "w");
    fputs("bonjour depuis csf", fichier);
    fclose(fichier);
    

    fichier = fopen("test.txt", "a");
    fputs(" et bienvenue ", fichier);
    fclose(fichier);*/

    return 0;
}
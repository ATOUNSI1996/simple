#include <stdio.h>
#include <math.h>

int saisie(int x)
{
    do
    {
        printf("entrer un nombre positif : \n");
        scanf("%d", &x);
    } while (x<0);
}

int main() 
{
   int saisie(int x);
}